import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'meu-portfolio',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
